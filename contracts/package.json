{
  "name": "holons-local",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ holons-local",
    "create-local": "graph create --node http://localhost:8020/ holons-local",
    "remove-local": "graph remove --node http://localhost:8020/ holons-local",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 holons-local",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.94.0",
    "@graphprotocol/graph-ts": "0.36.0"
  },
  "devDependencies": { "matchstick-as": "0.6.0" }
}
